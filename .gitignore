# Virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so

# User data (persisted locally, not committed)
shopping_list.json

# Environment files (may contain secrets)
.env
.env.local
.env.production

# Azure Functions local settings (may contain secrets)
api/local.settings.json

# Foundry deployment artifacts (binary zips)
.foundry/direct-code/agent-code.zip

# Temp echo/test files
echo_main.py
echo_requirements.txt
server_echo.py
Dockerfile.echo

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# SWA CLI
swa-cli.config.json
